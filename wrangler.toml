name = "track-tag"
pages_build_output_dir = "."
compatibility_date = "2024-09-01"

# Para desarrollo local con `wrangler pages dev`.
# En producción, el binding se define desde el dashboard de Cloudflare Pages
# (Settings > Functions > KV namespace bindings), no desde este archivo.
[[kv_namespaces]]
binding = "INVENTORY_KV"
id = "REEMPLAZAR_CON_ID_DEL_NAMESPACE"
